fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
groq>=0.5.0
sqlalchemy>=2.0.25
python-dotenv>=1.0.1
fastembed>=0.2.7

# Optional: only needed if you point DATABASE_URL at Postgres instead of the
# default SQLite file. Not required for local/dev use.
psycopg2-binary>=2.9.9
